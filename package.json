{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "iina",
  "title": "IINA",
  "description": "Play a URL or file in IINA",
  "icon": "extension-icon.png",
  "author": "xxxxiehf",
  "license": "MIT",
  "categories": [
    "Media"
  ],
  "commands": [
    {
      "name": "play",
      "title": "Play in IINA",
      "subtitle": "IINA",
      "description": "Open a URL or file path in IINA and start playing",
      "mode": "no-view",
      "arguments": [
        {
          "name": "url",
          "placeholder": "URL or file path",
          "type": "text",
          "required": false
        }
      ]
    }
  ],
  "preferences": [
    {
      "name": "iinaPath",
      "type": "appPicker",
      "title": "IINA Application",
      "description": "The IINA app to launch (defaults to /Applications/IINA.app if left unset)",
      "required": false
    },
    {
      "name": "separateWindows",
      "type": "checkbox",
      "title": "Window Behavior",
      "label": "Open in a new window",
      "description": "Open each link in its own IINA window instead of reusing the current one",
      "default": true,
      "required": false
    },
    {
      "name": "musicMode",
      "type": "checkbox",
      "title": "Music Mode",
      "label": "Start in music mode",
      "description": "Enter IINA's music mode after opening the media",
      "default": false,
      "required": false
    },
    {
      "name": "extraArgs",
      "type": "textfield",
      "title": "Extra iina-cli Arguments",
      "description": "Additional space-separated iina-cli/mpv flags, e.g. --mpv-volume=80",
      "placeholder": "--mpv-volume=80",
      "required": false
    }
  ],
  "dependencies": {
    "@raycast/api": "^2.3.1",
    "@raycast/utils": "^2.3.1"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2",
    "@types/node": "^22",
    "@types/react": "^19",
    "eslint": "^9",
    "prettier": "^3",
    "typescript": "^5"
  },
  "scripts": {
    "build": "ray build -e dist",
    "dev": "ray develop",
    "lint": "ray lint",
    "fix-lint": "ray lint --fix",
    "publish": "ray publish"
  }
}
